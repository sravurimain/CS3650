public class Payroll {
    public Payroll() {
        // no-arg constructor
    }

    public void processPaymentList(Worker worker) {
        System.out.println("Payment processed for worker " + worker.getName());
    }
}
